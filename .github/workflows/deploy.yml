name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy
  cancel-in-progress: false

permissions:
  contents: read

env:
  IMAGE_NAME: fastlog-wms
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}
  AZURE_RG: ${{ secrets.AZURE_RESOURCE_GROUP }}
  AZURE_APP: ${{ secrets.AZURE_CONTAINERAPP }}

jobs:
  test:
    name: Odoo-Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: odoo
          POSTGRES_PASSWORD: odoo
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U odoo"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4

      - name: Build image
        run: docker build -f docker/Dockerfile.prod -t ${{ env.IMAGE_NAME }}:ci .

      - name: Run Odoo tests
        run: |
          docker run --rm --network host \
            ${{ env.IMAGE_NAME }}:ci \
            -d test_db \
            --db_host=localhost --db_port=5432 \
            --db_user=odoo --db_password=odoo \
            --test-enable --stop-after-init \
            -i fastlog_wms --without-demo=all \
            --log-level=test

  deploy:
    name: Build, Push & Deploy
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: meta
        run: echo "image_tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: ACR login
        run: az acr login --name "${ACR_LOGIN_SERVER%%.*}"

      - name: Build & push image
        run: |
          IMAGE="$ACR_LOGIN_SERVER/$IMAGE_NAME"
          TAG="${{ steps.meta.outputs.image_tag }}"
          docker build -f docker/Dockerfile.prod \
            -t "$IMAGE:$TAG" \
            -t "$IMAGE:latest" .
          docker push "$IMAGE:$TAG"
          docker push "$IMAGE:latest"

      - name: Update Container App
        run: |
          az containerapp update \
            --name "$AZURE_APP" \
            --resource-group "$AZURE_RG" \
            --image "$ACR_LOGIN_SERVER/$IMAGE_NAME:${{ steps.meta.outputs.image_tag }}"

      - name: Summary
        run: |
          echo "### Deployed :rocket:" >> "$GITHUB_STEP_SUMMARY"
          echo "Image: \`$ACR_LOGIN_SERVER/$IMAGE_NAME:${{ steps.meta.outputs.image_tag }}\`" >> "$GITHUB_STEP_SUMMARY"
